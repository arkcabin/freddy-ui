/**
 * Boneyard Skeleton Registry (Placeholder)
 * 
 * This file is automatically populated by running `npm run boneyard`.
 * It stores the snapshot data used to render pixel-perfect skeletons.
 */

import { registerBones } from "boneyard-js/react";

// Initialize with empty bones to allow the project to compile.
// Real data will be injected here during the next `boneyard build` run.
registerBones({});
